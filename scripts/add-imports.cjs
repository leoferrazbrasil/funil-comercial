const fs = require('fs'); const uiComponents = ['HeroPanel', 'PageIntro', 'MetricCard', 'Panel', 'ActionItem', 'TablePanel', 'EmptyState', 'Modal', 'ContactModal', 'LeadModal', 'OpportunityModal', 'MessageModal', 'ChannelModal', 'EntityForm', 'TextField', 'SelectField', 'LoadingScreen']; const pages = ['src/App.tsx', 'src/pages/Dashboard.tsx', 'src/pages/Inbox.tsx', 'src/pages/Contacts.tsx', 'src/pages/Leads.tsx', 'src/pages/Pipeline.tsx']; pages.forEach(p => { if(fs.existsSync(p)) { let content = fs.readFileSync(p, 'utf8'); const importPath = p.includes('pages') ? '../components/SharedUI' : './components/SharedUI'; content = 'import { ' + uiComponents.join(', ') + ' } from \"' + importPath + '\";\n' + content; fs.writeFileSync(p, content); } }); console.log('Imports added!');